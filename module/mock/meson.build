mock_lib = library('mock', 'mock.cpp', include_directories: [
                        appconf_includes,
                        ticket_includes,
                        utils_includes,
                        database_includes,
                   ]
)
mock_includes = include_directories('.')